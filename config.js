// Public Supabase settings. These two values are SAFE to commit: they identify
// your project, and the database rules in supabase/schema.sql are what protect
// the data. Never put your "service_role" / secret key in this file.
//
// Find both in the Supabase dashboard: Project Settings > API.
window.APP_CONFIG = {
  supabaseUrl: '',        // e.g. 'https://abcdefghijkl.supabase.co'
  supabaseAnonKey: '',    // the "anon" (or "publishable") key
};
